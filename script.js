const btn=document.querySelector('.menu-toggle'),nav=document.querySelector('.main-nav');btn?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.main-nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelector('#form')?.addEventListener('submit',e=>{e.preventDefault();const note=document.querySelector('#note');if(note)note.textContent='Đã ghi nhận trên website demo. Bước tiếp theo sẽ kết nối biểu mẫu với email/CRM để nhận dữ liệu thực tế.';e.target.reset();});