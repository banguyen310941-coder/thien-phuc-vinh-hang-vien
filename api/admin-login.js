const crypto=require('crypto');
function sign(exp,secret){return crypto.createHmac('sha256',secret).update(String(exp)).digest('hex')}
module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({ok:false});const {username,password}=req.body||{};const u=process.env.ADMIN_USERNAME,p=process.env.ADMIN_PASSWORD,s=process.env.ADMIN_SESSION_SECRET;if(!u||!p||!s)return res.status(500).json({ok:false,message:'Chưa cấu hình tài khoản quản trị trên Vercel.'});if(username!==u||password!==p)return res.status(401).json({ok:false,message:'Sai tài khoản hoặc mật khẩu.'});const exp=Date.now()+8*60*60*1000;const token=exp+'.'+sign(exp,s);res.setHeader('Set-Cookie',`tp_admin=${token}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=28800`);return res.status(200).json({ok:true})};